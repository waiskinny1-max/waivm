; fibonacci: compute F(10) = 55

mov r0, 10     ; n
mov r1, 0      ; a
mov r2, 1      ; b

loop:
  jz r0, done
  mov r3, r1
  add r3, r2
  mov r1, r2
  mov r2, r3
  sub r0, 1
  jmp loop

done:
  print r1
  halt
