; remainder example: sum odd numbers below 10

mov r0, 9       ; counter
mov r1, 0       ; sum

loop:
  mov r2, r0
  mod r2, 2
  cmp r2, 1
  jne skip
  add r1, r0
skip:
  sub r0, 1
  jnz r0, loop

print r1
halt
